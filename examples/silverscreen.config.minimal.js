/**
 * Minimal Silverscreen Configuration
 *
 * This config uses no plugins and only the default settings.
 * You can rename this to 'silverscreen.config.js' to use it.
 */

export default {
  // No plugins - just pure screenshot capture
  plugins: [],

  // Optional: customize output directory
  outputDir: 'screenshots',
};
